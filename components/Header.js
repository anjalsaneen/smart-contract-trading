import React from "react";
import { Menu } from "semantic-ui-react";
import { Link } from "../routes";

const Header = () => {
  return (
    <Menu style={{ marginTop: "10px", justifyContent: 'center', alignItems: 'center' }}>
        <div>Trade Shift</div>
    </Menu>
  );
};

export default Header;
